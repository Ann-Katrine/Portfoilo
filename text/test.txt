 let $Data = data[0].toString();
            var $imgData = $Data.split(","); 
            var $img = $imgData[0];
            var $beskrivelse = $imgData[1];
            var $tekst = document.createElement("p");
            var teksten = document.createTextNode($img + " mellemrum " + $beskrivelse);
            $tekst.appendChild(teksten);
            $("#listeTing")
                .append($img)
                .append($beskrivelse);